project_prefix = "salini-automation"   #"Your project identifier name in lowercase letters only - this will be applied as a prefix to all assets"
resource_owner = "salini"
# aws_region = "ap-south-1"
# azs = ["${var.AWS_REGION}a", "${var.AWS_REGION}b"]